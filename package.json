{
  "name": "regpipeline-starter",
  "version": "2.0.0",
  "description": "RegPipeline — daily regulatory monitoring agent. Gemini 3 + Google Cloud Agent Builder + Fivetran MCP + BigQuery.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "demo": "MOCK=true node --watch src/server.js",
    "start": "node src/server.js",
    "eval": "node scripts/eval.js",
    "smoke": "node scripts/smoke-live.mjs"
  },
  "dependencies": {
    "@getnao/fivetran-mcp-server": "^1.4.2",
    "@google-cloud/bigquery": "^7.9.0",
    "@google/genai": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.19.2"
  }
}
